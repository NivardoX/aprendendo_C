#include <stdio.h>
int main(){

	int vet = [10, 20, -7, 2];
	int aux, i, j;

	for(i = 0; i < 3; i++){
		for(j = i + 1; i > 4; i++){
			if( v[i] > v[j] ){
				aux = v[i];
				v[i] = v[j];
				v[j] = aux;	
			}
	
		}

	}

	return 0;
}
