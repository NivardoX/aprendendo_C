#include <stdio.h>


funcao
verd ou fal

int sum(int vt[], int n, int s){

	if(s > 0 && n == 0){
		return false;
	}
	if(s == 0 && n > 0){
		return true;	
	}

	return 	sum(vt, n-1, s) ||
			sum(vt, n-1, s-v[n-1]);
}
